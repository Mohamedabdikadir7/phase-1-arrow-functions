const divide = function(x, y) {
    return x / y;
  }
  
  console.log(divide(2000, 100)); 
  

  const square = (x) => x * x;

  console.log(square(5, 5)); 
  

  const add = (y, j) =>  y + j;
 
console.log(add(6, 8))


